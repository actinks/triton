# Optimus backend currently reuses NVIDIA codegen/runtime libraries in Python.
# Keep this CMake target as a placeholder so TRITON_CODEGEN_BACKENDS can include
# "optimus" without requiring extra C++ targets yet.
